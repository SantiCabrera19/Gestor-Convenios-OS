# Configuración de Supabase (Base de Datos y Auth)
# Si usas otro proveedor, necesitarás adaptar el cliente de base de datos.
NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu-clave-anonima-publica
SUPABASE_SERVICE_ROLE_KEY=tu-clave-service-role-secreta

# Configuración de Almacenamiento
# Opciones: 'local' (default) | 'drive'
STORAGE_PROVIDER=local

# Configuración de Google (Para Auth y Drive)
# Necesario para el inicio de sesión y la subida de archivos a Drive (si STORAGE_PROVIDER=drive).
GOOGLE_CLIENT_ID=tu-google-client-id
GOOGLE_CLIENT_SECRET=tu-google-client-secret
GOOGLE_REDIRECT_URI=https://tu-proyecto.supabase.co/auth/v1/callback

# Configuración General
NEXT_PUBLIC_APP_URL=http://localhost:3000
